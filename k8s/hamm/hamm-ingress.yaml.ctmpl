apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hamm-ingress
spec:
  rules:
  - http:
      paths:
      - path: /status
        backend:
          serviceName: hamm-cost-updater
          servicePort: http