{{with $environment := env "ENVIRONMENT"}}
{{with $sa := vault (printf "secret/dsde/firecloud/%s/hamm-cost-updater/hamm-cloudsql-proxy.json" $environment)}}
apiVersion: v1
kind: Secret
metadata:
  name: cloudsql-sa
type: Opaque
stringData:
  credentials.json: |-
    {{$sa.Data | toJSON}}
{{end}}{{end}}